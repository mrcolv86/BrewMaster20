{
  "name": "brewmaster20",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development tsx server/index.ts",
    "build": "vite build && drizzle-kit push && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.5.0",
    "pg": "^8.11.3",
    "drizzle-orm": "0.29.5",
    "drizzle-zod": "^0.7.1"
  },
  "devDependencies": {
    "drizzle-kit": "0.19.17",
    "tsx": "^4.0.0",
    "vite": "^5.0.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.25.0",
    "cross-env": "^7.0.3"
  }
}